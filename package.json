{
  "name": "depxray-root",
  "private": true,
  "version": "1.0.0",
  "description": "Scan and visualize JavaScript and TypeScript project structure and dependency graphs — usable as a library, CLI, and AI agent tool.",
  "workspaces": [
    "packages/core",
    "packages/cli",
    "packages/web-ui"
  ],
  "scripts": {
    "version:sync": "node ./scripts/sync-versions.mjs",
    "build": "npm run version:sync && npm run build --workspace @depxray/core && npm run build --workspace @depxray/web-ui && npm run build --workspace depxray",
    "test": "npm run test --workspaces --if-present",
    "clean": "npm run clean --workspaces --if-present"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "author": "Pannawish Kriengyakul",
  "license": "MIT"
}
