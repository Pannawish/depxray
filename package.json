{
  "name": "depxray-root",
  "private": true,
  "version": "3.1.0",
  "description": "Codebase intelligence for JavaScript and TypeScript with dependency graphs, health scoring, impact analysis, cleanup automation, CI checks, MCP tools, and AI-agent context.",
  "workspaces": [
    "packages/core",
    "packages/cli",
    "packages/web-ui",
    "packages/mcp"
  ],
  "scripts": {
    "version:sync": "node ./scripts/sync-versions.mjs",
    "typecheck": "npm run build --workspace @depxray/core && npm run typecheck --workspace depxray --workspace @depxray/web-ui --workspace @depxray/mcp",
    "build": "npm run version:sync && npm run typecheck && npm run build --workspace @depxray/core && npm run build --workspace @depxray/web-ui && npm run build --workspace @depxray/mcp && npm run build --workspace depxray",
    "test": "npm run test --workspaces --if-present",
    "clean": "npm run clean --workspaces --if-present"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "author": "Pannawish Kriengyakul",
  "license": "MIT"
}
