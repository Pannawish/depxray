{
  "name": "depxray-root",
  "private": true,
  "version": "0.3.1",
  "description": "Scan and visualize JavaScript and TypeScript project structure and dependency graphs — usable as a library, CLI, and AI agent tool.",
  "workspaces": [
    "packages/core",
    "packages/cli",
    "packages/web-ui"
  ],
  "scripts": {
    "build": "npm run build --workspace @depxray/core && npm run build --workspace @depxray/web-ui && npm run build --workspace depxray",
    "test": "npm run test --workspaces --if-present",
    "clean": "npm run clean --workspaces --if-present",
    "lint": "eslint packages/*/src/**/*.ts"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "author": "Pannawish Kriengyakul",
  "license": "MIT"
}
