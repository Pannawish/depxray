{
  "name": "depxray-root",
  "private": true,
  "version": "2.1.0",
  "description": "Dependency intelligence for JavaScript and TypeScript codebases, with browser graphs, architecture rules, graph diffs, reports, MCP, and AI-agent context.",
  "workspaces": [
    "packages/core",
    "packages/cli",
    "packages/web-ui",
    "packages/mcp"
  ],
  "scripts": {
    "version:sync": "node ./scripts/sync-versions.mjs",
    "build": "npm run version:sync && npm run build --workspace @depxray/core && npm run build --workspace @depxray/web-ui && npm run build --workspace @depxray/mcp && npm run build --workspace depxray",
    "test": "npm run test --workspaces --if-present",
    "clean": "npm run clean --workspaces --if-present"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "author": "Pannawish Kriengyakul",
  "license": "MIT"
}
