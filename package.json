{
  "name": "rdg-workspace",
  "private": true,
  "version": "0.1.0",
  "description": "Scan React projects and explore structure and dependency graphs in the browser, CLI, or JSON exports for AI agents.",
  "workspaces": [
    "packages/core",
    "packages/cli",
    "packages/web-ui"
  ],
  "scripts": {
    "build": "npm run build --workspace @rdg/core && npm run build --workspace @rdg/web-ui && npm run build --workspace ./packages/cli",
    "test": "npm run test --workspaces --if-present",
    "clean": "npm run clean --workspaces --if-present",
    "lint": "eslint packages/*/src/**/*.ts"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "author": "",
  "license": "MIT"
}
